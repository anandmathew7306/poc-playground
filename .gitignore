__pycache__/
*.pyc

# generated reports contain live cluster data — never commit
report.md
*-report.md
*.report.md

# local env files contain credentials and client identifiers — never commit
.env

# per-machine cluster catalog (copy from clusters.yaml.example)
cluster-login/clusters.yaml

# Talos/AWS lab secrets and generated machine configs — never commit
talos-gpu-aws/kubeconfig
talos-gpu-aws/talosconfig
talos-gpu-aws/controlplane.yaml
talos-gpu-aws/worker.yaml
