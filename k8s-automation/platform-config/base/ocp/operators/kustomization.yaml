apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Operator subscriptions are staged here. Uncomment cluster-logging-subscription.yaml
# once the openshift-logging namespace and OperatorGroup exist on the cluster.
resources: []

commonLabels:
  platform.io/team: platrel
  platform.io/managed-by: kustomize
